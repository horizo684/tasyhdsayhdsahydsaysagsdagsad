# Инструкция по сборке AmethystClient

## Метод 1: Сборка через Gradle (рекомендуется)

### Требования:
- Java JDK 8
- Gradle (или используй gradlew)

### Шаги:

1. **Распакуй проект** в любую папку

2. **Открой терминал** в папке проекта

3. **Настрой окружение** (первый раз):
   ```bash
   ./gradlew setupDecompWorkspace
   ```
   Это займет несколько минут

4. **Собери мод**:
   ```bash
   ./gradlew build
   ```

5. **Найди готовый .jar** в:
   ```
   build/libs/AmethystClient-1.1.jar
   ```

6. **Установи мод**:
   - Скопируй .jar в папку `.minecraft/mods/`
   - Запусти Minecraft с Forge 1.8.9

---

## Метод 2: Ручная сборка (если нет Gradle)

### Требования:
- Java JDK 8
- Minecraft Forge 1.8.9 (установленный)

### Шаги:

1. **Компиляция исходников**:
   ```bash
   javac -cp "путь_к_forge/minecraft_server.1.8.9.jar;путь_к_forge/forge-1.8.9.jar" \
         -d out \
         src/main/java/com/amethyst/client/*.java \
         src/main/java/com/amethyst/client/modules/*.java
   ```

2. **Создание JAR архива**:
   ```bash
   cd out
   jar cvf AmethystClient-1.1.jar com/
   ```

3. **Добавление ресурсов**:
   - Открой .jar с помощью архиватора (7zip, WinRAR)
   - Добавь файл `mcmod.info` в корень архива

4. **Установка**:
   - Скопируй .jar в `.minecraft/mods/`

---

## Решение проблем

### Ошибка: "Cannot find symbol"
- Убедись, что используешь Java 8
- Проверь, что Forge правильно установлен

### Ошибка при setupDecompWorkspace
```bash
# Попробуй очистить кэш:
./gradlew clean
./gradlew setupDecompWorkspace --refresh-dependencies
```

### Мод не загружается
- Проверь версию Forge (должна быть 1.8.9)
- Посмотри логи в `.minecraft/logs/fml-client-latest.log`

---

## Быстрые команды

```bash
# Полная пересборка
./gradlew clean build

# Только компиляция
./gradlew compileJava

# Запуск тестового клиента
./gradlew runClient
```

---

## Настройка IDE

### IntelliJ IDEA:
```bash
./gradlew idea
# Затем: File → Open → выбери папку проекта
```

### Eclipse:
```bash
./gradlew eclipse
# Затем: File → Import → Existing Projects
```

---

**После сборки не забудь протестировать все функции!** 🎮
