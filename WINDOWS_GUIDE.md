# 🪟 ИНСТРУКЦИЯ ДЛЯ WINDOWS

## ❗ ПРОБЛЕМА РЕШЕНА!

Теперь в архиве есть `gradlew.bat` для Windows!

---

## 🚀 СПОСОБ 1: С Gradle Wrapper (РЕКОМЕНДУЕТСЯ)

### Шаг 1: Распакуй архив
Распакуй `AmethystClient-v1.1-Source.zip` в любую папку

### Шаг 2: Открой командную строку
1. Открой папку `AmethystClient`
2. В адресной строке напиши `cmd` и нажми Enter
3. Или: Shift + ПКМ → "Открыть окно команд здесь"

### Шаг 3: Настрой окружение (первый раз)
```cmd
gradlew.bat setupDecompWorkspace
```
⏱️ Это займет 5-15 минут (скачивает зависимости)

### Шаг 4: Собери мод
```cmd
gradlew.bat build
```
⏱️ Займет 2-5 минут

### Шаг 5: Установи
Готовый мод будет здесь:
```
build\libs\AmethystClient-1.1.jar
```

Скопируй его в:
```
%APPDATA%\.minecraft\mods\
```

**Готово!** 🎉

---

## 🛠️ СПОСОБ 2: Установка Gradle (если способ 1 не работает)

### Шаг 1: Установи Java JDK 8
1. Скачай с https://www.oracle.com/java/technologies/javase/javase-jdk8-downloads.html
2. Установи
3. Добавь в PATH:
   - Панель управления → Система → Дополнительные параметры
   - Переменные среды → PATH
   - Добавь: `C:\Program Files\Java\jdk1.8.0_xxx\bin`

### Шаг 2: Установи Gradle
1. Скачай Gradle 2.14 с https://gradle.org/releases/
2. Распакуй в `C:\Gradle`
3. Добавь в PATH: `C:\Gradle\gradle-2.14\bin`

### Шаг 3: Проверь установку
```cmd
java -version
gradle -version
```

### Шаг 4: Собери проект
```cmd
cd путь\до\AmethystClient
gradle setupDecompWorkspace
gradle build
```

---

## 🎯 СПОСОБ 3: Ручная компиляция (без Gradle)

### Требования:
- Java JDK 8
- Minecraft Forge 1.8.9 (установленный)

### Инструкция:

#### 1. Найди Forge библиотеки
После установки Forge 1.8.9, библиотеки будут здесь:
```
%USERPROFILE%\.gradle\caches\modules-2\files-2.1\
```

Или в установленном Forge:
```
versions\1.8.9-forge1.8.9-xxx\
```

#### 2. Скомпилируй исходники
```cmd
cd AmethystClient\src\main\java

javac -cp "путь\к\forge.jar;путь\к\minecraft.jar" ^
      -d ..\..\..\build\classes ^
      com\amethyst\client\*.java ^
      com\amethyst\client\modules\*.java
```

#### 3. Создай JAR
```cmd
cd ..\..\..\build\classes
jar cvf AmethystClient-1.1.jar com\
```

#### 4. Добавь ресурсы
Открой JAR с помощью 7zip/WinRAR и добавь `mcmod.info` в корень

---

## 💡 СПОСОБ 4: Используй готовую IDE

### IntelliJ IDEA (рекомендуется):

1. **Скачай и установи** IntelliJ IDEA Community (бесплатно)
   https://www.jetbrains.com/idea/download/

2. **Открой проект:**
   - File → Open
   - Выбери папку `AmethystClient`

3. **Импортируй Gradle:**
   - IntelliJ автоматически обнаружит build.gradle
   - Нажми "Import Gradle Project"
   - Подожди пока загрузятся зависимости

4. **Собери проект:**
   - Gradle панель справа
   - Tasks → build → build
   - Двойной клик

5. **Готово!**
   Файл будет в `build\libs\AmethystClient-1.1.jar`

---

### Eclipse:

1. **Скачай Eclipse для Java**
   https://www.eclipse.org/downloads/

2. **Открой терминал в папке проекта:**
   ```cmd
   gradlew.bat eclipse
   ```

3. **Импортируй в Eclipse:**
   - File → Import → Existing Projects
   - Выбери папку `AmethystClient`

4. **Собери:**
   - ПКМ на проект → Build Project

---

## 🐛 РЕШЕНИЕ ПРОБЛЕМ

### "java не является командой"
**Решение:** Установи Java JDK 8 и добавь в PATH

### "JAVA_HOME не установлен"
**Решение:** 
```cmd
setx JAVA_HOME "C:\Program Files\Java\jdk1.8.0_xxx"
```

### "Не удается найти или загрузить главный класс"
**Решение:** Проверь что используешь JDK (не JRE)

### gradlew.bat зависает
**Решение:** Дай ему время, первый запуск медленный (скачивает зависимости)

### Ошибка "Could not resolve dependencies"
**Решение:** 
1. Проверь интернет соединение
2. Попробуй:
```cmd
gradlew.bat clean
gradlew.bat setupDecompWorkspace --refresh-dependencies
```

---

## ⚡ БЫСТРАЯ СБОРКА (одна команда)

Открой `cmd` в папке проекта и выполни:

```cmd
gradlew.bat clean setupDecompWorkspace build
```

Это сделает всё за раз! ⏱️ Займет 10-20 минут в первый раз

---

## 📁 КУДА КЛАСТЬ МОД

После сборки скопируй:
```
build\libs\AmethystClient-1.1.jar
```

В:
```
%APPDATA%\.minecraft\mods\
```

Или быстрый способ:
1. Нажми Win+R
2. Введи: `%appdata%\.minecraft\mods`
3. Скопируй туда .jar файл

---

## ✅ ПРОВЕРКА УСТАНОВКИ

1. Запусти Minecraft Launcher
2. Выбери профиль с Forge 1.8.9
3. Запусти игру
4. В главном меню внизу слева должно быть "X mods loaded"
5. Нажми "Mods" - в списке должен быть AmethystClient

В игре:
- Нажми **Right Shift** - должно открыться GUI
- В левом верхнем углу должна быть надпись "AmethystClient"

---

## 🎮 УПРАВЛЕНИЕ

После установки:
- **RShift** - Открыть GUI
- **ЛКМ** в GUI - Включить/выключить модуль
- **← →** в GUI - Сменить градиент
- **Колесико** на игрока - Добавить друга
- **F5** - Увидеть "СИЛЬНЕЙШИЙ ИГРОК"

---

## 📞 ВСЁ ЕЩЕ НЕ РАБОТАЕТ?

### Вариант 1: Используй IntelliJ IDEA (проще всего)
Он сделает всё автоматически!

### Вариант 2: Попроси собрать мод
Если совсем не получается, можешь попросить кого-то собрать .jar файл за тебя

### Вариант 3: Используй готовую сборку Forge
Некоторые сборки Forge идут с готовым окружением для разработки

---

**УДАЧИ! 🎮**

P.S. Первая сборка всегда самая долгая, потом будет быстрее!
